# Product-Inventory-REST-API-
A RESTful Product Inventory API built with Node.js and Express.js that performs CRUD operations, search, filtering, input validation, and inventory value calculations using an in-memory JavaScript array.
